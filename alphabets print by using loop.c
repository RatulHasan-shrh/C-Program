

#include <stdio.h>

int main() {
    char ch;

    for (ch = 'A'; ch <= 'Z'; ch++) {
        printf("%c ", ch);
    }

    return 0;
}


✅ Print a to z using a loop
c
Copy code
#include <stdio.h>

int main() {
    char ch;

    for (ch = 'a'; ch <= 'z'; ch++) {
        printf("%c ", ch);
    }

    return 0;
}
